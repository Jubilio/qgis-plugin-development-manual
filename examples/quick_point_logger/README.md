# Quick Point Logger Example

Educational plugin accompanying the manual. It captures clicks, transforms coordinates to EPSG:4326, stores a note, supports undo and exports GeoJSON.

This example is intentionally smaller than GeoClick Capture and is not intended to replace it.
